on:
    push:
        branches:
            - main

jobs:
    create-new-release:
        strategy:
            matrix:
                os: [ubuntu-latest, windows-latest, macos-latest]
        runs-on: {{ matrix.os }}
        steps:
            - run: echo "Create release for " {{matrix.os}}